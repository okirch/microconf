uc_add_option_with ruby
uc_with_ruby=detect

uc_add_help <<EOH
  Override ruby detection
        --with-ruby=VERSION (does not work right now)
        --without-ruby
                The default behavior is to attempt autodetection
                of the installed interpreter version and path
 
EOH
